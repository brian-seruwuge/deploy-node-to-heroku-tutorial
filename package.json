{
  "name": "heroku_deployment",
  "version": "1.0.0",
  "description": "example on deployment of app on heroku",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "brian",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
